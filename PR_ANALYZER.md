# 🤖 PR Analyzer - Автоматический анализ Pull Request

## Описание

PR Analyzer - это интеллектуальный инструмент для автоматического анализа Pull Request с использованием:

- **RAG (Retrieval-Augmented Generation)** - использует индексированную документацию проекта для контекста
- **MCP (Model Context Protocol)** - получает diff и файлы из git репозитория через стандартизированный протокол
- **Статический анализ** - проверяет код на потенциальные проблемы, уязвимости и лучшие практики

## Возможности

### Автоматическое обнаружение проблем

1. **Безопасность** (HIGH приоритет)
   - SQL injection
   - XSS уязвимости
   - Пароли и API ключи в коде
   - Использование небезопасных функций (`eval`, `exec`, etc.)

2. **Потенциальные баги** (MEDIUM приоритет)
   - Неправильные операторы сравнения (`=` вместо `==`)
   - Null pointer exceptions
   - Пустые catch блоки
   - Проблемы с синхронизацией

3. **Стиль кода и документация** (рекомендации)
   - TODO/FIXME комментарии
   - Отладочные выводы (console.log, println)
   - Отсутствие документации у публичных функций
   - Слишком большие изменения

4. **Контекстный анализ с RAG**
   - Использует документацию проекта для проверки соответствия архитектуре
   - Предлагает улучшения на основе best practices проекта

## Установка и настройка

### 1. Предварительные требования

- JDK 17+
- Gradle 8+
- Git
- (Опционально) GitHub CLI для анализа PR через API

### 2. Сборка проекта

```bash
./gradlew build
```

### 3. Индексация документации

Для использования RAG сначала проиндексируйте документацию:

```bash
./gradlew indexDocs
```

Это создаст векторный индекс из файлов в `project/` директории.

## Использование

### Локальный анализ (рекомендуется)

Используйте удобный скрипт `analyze-pr.sh`:

```bash
# Анализ текущей ветки vs main
./analyze-pr.sh

# Анализ конкретной ветки vs main
./analyze-pr.sh feature-branch

# Анализ между двумя ветками
./analyze-pr.sh main feature-branch

# Анализ PR через GitHub API
./analyze-pr.sh --pr 123

# Кастомные параметры
./analyze-pr.sh --base main --head feature --output my-review.md
```

### Через Gradle напрямую

```bash
# Анализ текущей ветки
./gradlew analyzePr

# С параметрами
./gradlew analyzePr -Pbase=main -Phead=feature-branch

# Анализ PR
./gradlew analyzePr -Ppr=123

# JSON формат
./gradlew analyzePr -Pformat=json -Poutput=result.json
```

### Параметры

- `-Pbase=<branch>` - базовая ветка (по умолчанию: main)
- `-Phead=<branch>` - сравниваемая ветка (по умолчанию: текущая)
- `-Ppr=<number>` - номер PR для анализа через GitHub API
- `-Pformat=<text|json>` - формат вывода (по умолчанию: text)
- `-Poutput=<file>` - файл для сохранения результата

## GitHub Actions (CI/CD)

### Автоматический анализ всех PR

Workflow `.github/workflows/pr-review.yml` автоматически запускается при:
- Создании нового PR
- Обновлении PR (новые коммиты)
- Повторном открытии PR

### Что делает workflow

1. Клонирует репозиторий с полной историей
2. Настраивает JDK 17 и Gradle
3. Собирает проект
4. Индексирует документацию
5. Анализирует изменения в PR
6. Публикует результат как комментарий к PR
7. Обновляет комментарий при новых коммитах

### Настройка для вашего проекта

1. Скопируйте `.github/workflows/pr-review.yml` в ваш репозиторий

2. Убедитесь что GitHub Actions имеет разрешения:
   - Settings → Actions → General → Workflow permissions
   - Выберите "Read and write permissions"

3. (Опционально) Настройте блокировку мержа при критических проблемах:
   ```yaml
   - name: Check for critical issues
     run: exit 1  # Изменить с 0 на 1
   ```

## Структура проекта

```
dev-assistant/
├── src/main/kotlin/assistant/
│   ├── pr/
│   │   ├── PrAnalyzer.kt      # Основной анализатор
│   │   └── PrAnalyzerCli.kt   # CLI интерфейс
│   ├── mcp/
│   │   └── GitMcpServer.kt    # MCP сервер с git инструментами
│   └── rag/
│       ├── DocumentStore.kt   # RAG хранилище
│       └── Embeddings.kt      # Векторные эмбеддинги
├── .github/workflows/
│   └── pr-review.yml          # GitHub Actions workflow
├── analyze-pr.sh              # Удобный скрипт для локального использования
└── build.gradle.kts
```

## MCP Tools для PR анализа

Расширенный MCP сервер предоставляет следующие инструменты:

### git_diff
Получает diff между ветками
```json
{
  "name": "git_diff",
  "arguments": {
    "base": "main",
    "head": "feature-branch"
  }
}
```

### git_changed_files
Список измененных файлов со статусами
```json
{
  "name": "git_changed_files",
  "arguments": {
    "base": "main",
    "head": "feature-branch"
  }
}
```

### git_show_file
Содержимое файла из репозитория
```json
{
  "name": "git_show_file",
  "arguments": {
    "file": "src/main.kt",
    "ref": "main"
  }
}
```

## Примеры результатов

### Успешный анализ без проблем

```
## Анализ Pull Request

### Общая статистика
- Измененных файлов: 3
- Всего изменений: 45 строк

### Найденные проблемы
✅ Критических проблем не обнаружено

### Рекомендации
- Всего рекомендаций: 2
```

### Анализ с проблемами

```
## Анализ Pull Request

### Общая статистика
- Измененных файлов: 5
- Всего изменений: 127 строк

### Найденные проблемы
- 🔴 Высокий приоритет: 2
- 🟡 Средний приоритет: 3

### Рекомендации
- Всего рекомендаций: 5

⚠️ Обнаружены проблемы высокого приоритета. Рекомендуется исправить перед мержем.

## 🔍 Найденные проблемы

### 🔴 Безопасность - src/api/users.kt:45
**Проблема:** Возможная SQL injection уязвимость

```kotlin
+    val query = "SELECT * FROM users WHERE id = " + userId
```

### 🟡 Потенциальный баг - src/utils/helper.kt:23
**Проблема:** Пустой catch блок скрывает ошибки

```kotlin
+    catch (e: Exception) { }
```

## 💡 Рекомендации

### 📝 Документация - src/api/service.kt
Публичные функции должны иметь KDoc документацию
```

## Конфигурация

### config.json

```json
{
  "llm_api_url": "http://localhost:11434",
  "llm_model": "llama3.2",
  "embedding_model": "nomic-embed-text",
  "docs_path": "project",
  "index_path": "data/index.json",
  "chunk_size": 500,
  "chunk_overlap": 50
}
```

Параметры:
- `docs_path` - путь к документации для индексации
- `index_path` - где хранить векторный индекс
- `chunk_size` - размер чанков для RAG
- `chunk_overlap` - перекрытие между чанками

## Расширение анализатора

### Добавление новых проверок

Редактируйте `PrAnalyzer.kt`, метод `analyzeFileChanges`:

```kotlin
// Добавить новый паттерн проверки
val newPatterns = mapOf(
    "myPattern" to "Описание проблемы"
)

for ((pattern, message) in newPatterns) {
    val regex = Regex(pattern, RegexOption.IGNORE_CASE)
    for ((index, change) in file.changes.withIndex()) {
        if (change.startsWith("+") && regex.containsMatchIn(change)) {
            issues.add(Issue(
                severity = IssueSeverity.HIGH,
                category = IssueCategory.SECURITY,
                file = file.path,
                line = index + 1,
                message = message,
                code = change.trim()
            ))
        }
    }
}
```

## Troubleshooting

### Проблема: "Git not available"
Убедитесь что git установлен и доступен в PATH:
```bash
git --version
```

### Проблема: "Нет изменений для анализа"
Проверьте что вы на правильной ветке и есть коммиты:
```bash
git status
git log --oneline -n 5
```

### Проблема: Индексация не работает
Проверьте что в `project/` есть файлы для индексации:
```bash
ls -la project/
```

### Проблема: GitHub Actions не комментирует PR
Проверьте разрешения:
- Settings → Actions → General → Workflow permissions
- Должно быть "Read and write permissions"

## Best Practices

1. **Регулярно обновляйте индекс документации**
   ```bash
   ./gradlew indexDocs
   ```

2. **Запускайте анализ локально перед пушем**
   ```bash
   ./analyze-pr.sh
   ```

3. **Настройте pre-push hook** (опционально)
   ```bash
   # .git/hooks/pre-push
   #!/bin/bash
   ./analyze-pr.sh --format json --output /tmp/analysis.json
   # Проверить результат и заблокировать push при критических проблемах
   ```

4. **Используйте JSON формат для интеграции с другими инструментами**
   ```bash
   ./gradlew analyzePr -Pformat=json -Poutput=result.json
   cat result.json | jq '.issues[] | select(.severity == "HIGH")'
   ```

## Roadmap

- [ ] Интеграция с LLM для более умного анализа
- [ ] Поддержка других VCS (не только git)
- [ ] Web UI для просмотра результатов
- [ ] Кастомные правила через конфигурацию
- [ ] Интеграция с популярными линтерами
- [ ] Автоматические исправления (auto-fix)

## Поддержка

Если у вас возникли проблемы или вопросы:
1. Проверьте раздел Troubleshooting выше
2. Посмотрите логи: `./gradlew analyzePr --stacktrace`
3. Создайте issue в репозитории
